<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The launch screen is painted before Flutter is running and so before
         the app knows anything about the theme. Without this, a phone in dark
         mode opened the app on a full white flash and then dropped into a
         near-black UI. Matches SmartTaxiPalette.dark.appBackground. -->
    <color name="baisapar_background">#05070C</color>
</resources>
