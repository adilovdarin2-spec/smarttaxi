name: Basic check
on:
  pull_request:
  push:
    branches: [ main, dev ]
jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show files
        run: find . -maxdepth 3 -type f | sort
