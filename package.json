{
  "name": "smarttaxi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "syntax": "npm --prefix apps/api run syntax",
    "check": "npm --prefix apps/api run check",
    "syntax:web": "npm --prefix apps/web run syntax",
    "check:web": "npm --prefix apps/web run check",
    "build": "npm --prefix apps/web run build"
  }
}
