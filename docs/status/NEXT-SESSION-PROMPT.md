# Промпт для следующей сессии (скопируй и отправь целиком)

Работай над `apps/mobile/smarttaxi_app/**` (клиентская + со временем водительская часть, как ниже) и `apps/api/**` там, где нужно бэкенду. Доведи приложение SmartTaxi до идеала — красиво, удобно, функционально, без единой недоделки. Не спрашивай меня ни о чём, я недоступен — принимай решения сам, документируй их в docs/status, коммить точечно (никогда `git add -A`), проверяй `flutter analyze`/`flutter test`/`npm run syntax` после каждого изменения.

## Что уже сделано (не переделывать, проверить что не сломано)

- Регион-пикер пассажира починен (`_RegionSelectSheet`/`_RegionConfirmSheet` подключены), добавлен пункт «Регион» в Настройки.
- SMS + WhatsApp через Infobip реализованы (`apps/api/src/modules/auth/{sms,whatsapp}.provider.js`), реальные ключи уже стоят на Railway.
- Экран «нужно обновление» (`main.dart`, `_UpdateRequiredScreen`/`_UpdateAvailableSheet`) + бэкенд `GET /api/app-version` — готов, работает через `AppConfig.appVersion`.
- Аватарка водителя (только с камеры, не из галереи) показывается в: driver-found/en-route (`_DriverContactCard`), в поездке, на экране отзыва (`_RateDriverPanel`), в деталях поездки (`_TripDetailScreen`). Проверь: не появились ли новые экраны с именем водителя без фото.
- Продакшен API стабилен (`docs/status/INCIDENT-2026-07-18-api-down.md` — полная история инцидента и решения, включая правило: перед любым деплоем гонять `git status --porcelain apps/api` и коммитить забытые файлы).
- Тема/меню пассажирской части (`passenger_shell.dart`) — полностью мигрированы на `context.palette`, подробности ниже.

## Тема и меню passenger_shell.dart — ЗАВЕРШЕНО (2026-07-19)

`passenger_shell.dart` полностью мигрирован на `context.palette` — корневой `Scaffold` (`_PassengerShellState.build()`) и ВСЕ 16 экранов, которые на нём висят (home/trips/profile/promoCodes/notifications/driverApplication/support/faq/about/legalHub/legalDocument×5/settings/recurringBookings/favoriteAddresses/driverPreferences/referrals), плюс `_PremiumCard` и все общие row/label-виджеты внутри карточек, плюс все ранее начатые самодостаточные шторки. Коммиты (все запушены в `dev`): `0aaa077`, `c94100d`, `8fa141b`, `3611ffc`, `cf96c02`, `0e47308`, `cab306e`, `5ccc6b3`, `afeb580`, `69a8139`, `8543a97`, `74cbf22`, `b6213b7`, `7525a93`, `3001a9b`, `fd055ce`, `b7a42ec`.

**Проверено механически (см. коммит `b7a42ec` для полного обоснования):** каждый перенесённый цвет — точная замена 1:1 `SmartTaxiColors.X` → `palette.X`, где `palette.light.X` побайтово совпадает со старым статичным значением (светлая тема гарантированно не изменилась), и каждая пара фон+контент читает из ОДНОГО И ТОГО ЖЕ `palette` внутри ОДНОГО build()-метода (тёмная тема всегда меняется синхронно).

**НЕ мигрировано (сознательно, задокументировано в коммитах):**
- `_CompactNotice`/`_InlineMessage` — сами красят свой фон, есть ad hoc hex-значения и вызовы внутри защищённых `_OrderSheet`/`_TariffSection`; оставлены статичными везде для единообразия (не станет хуже — это самодостаточные карточки, не текст-на-чужом-фоне).
- `_SectionLabel` (общий класс) — один вызов внутри защищённого `_TariffSection` с динамическим `dark`; 4 вызова в моих экранах теперь передают `dark: Theme.of(context).brightness == Brightness.dark` вместо трогания класса.
- `StatusPill`, `LegalSectionCard` (`core/widgets`, `core/legal`) — сквозные, используются и водительской частью, самодостаточны.
- `_GoldCtaButton`, map glass chrome (`_MapGlassChrome` и т.д.) — вероятно намеренно вне темы, не трогал.
- `_OrderSheet`/`_MapPointPickerSheet`/`_TariffSection`/`_CollapsibleOrderSheet`/`_AddressSearchSheet` — защищённый tariff/address-flow. `_AddressSearchSheet` отдельно проверен: его фон красит ВЫЗЫВАЮЩИЙ `showModalBottomSheet(backgroundColor: Colors.white)`, а не сам виджет — мигрировать нельзя без правки protected-зоны.
- `_TripDetailScreen`/`_LocationRequiredScreen` — у каждого СВОЙ Scaffold (не общий с остальными 16 экранами), можно мигрировать отдельно в будущем, не обязательно для этой задачи.

**Живая визуальная проверка (light/dark) НЕ выполнена.** По пути нашёл и починил один реальный баг (commit `8612a38`): `web/` вообще не был закоммичен (висел untracked), а его `index.html` использовал legacy `window.flutterConfiguration`, который конфликтует с новым `engineInitializer.initializeEngine()` API текущего Flutter SDK — падало с `Assertion failed: !_usedLegacyConfigStyle`. Убрал legacy-конфиг, `flutter run -d chrome` больше не падает.

Но остаётся ВТОРАЯ, более глубокая проблема: даже после этого фикса (и в `flutter run -d chrome`, и в `flutter build web` + статическая раздача) CanvasKit грузится (`window.flutterCanvasKit` не null), `<flutter-view>`/`<flt-glass-pane>` создаются, но `<flt-glass-pane>` остаётся с 0 детей навсегда — приложение НИКОГДА не рисует первый кадр (`document.querySelectorAll('canvas')` пусто бесконечно), без единой ошибки в консоли. Три гипотезы проверены и ОПРОВЕРГНУТЫ эмпирически:
1. Sentry — временно вырезал `SentryFlutter.init()` из `main.dart`, не помогло (сразу вернул код обратно, `git diff` чист).
2. Legacy CanvasKit-конфиг — почищен (см. выше), помог с другим багом (assertion crash), но не с этим.
3. Cross-origin-isolation — использовал `.claude`-конфиг `smarttaxi-mobile-coi` (кастомный node-сервер `coi-server.mjs`, шлёт `Cross-Origin-Opener-Policy`/`Cross-Origin-Embedder-Policy`), подтвердил `window.crossOriginIsolated === true` — тот же результат, 0 canvas.

`_flutter.loader.didCreateEngineInitializer` существует как функция (значит бутстрап дошёл до этой точки), но неизвестно, был ли он реально вызван Dart-кодом. Похоже на что-то в связке этого sandboxed Electron-браузера с Flutter web engine именно в районе первого кадра — не успел докопаться глубже (следующий шаг — проверить Dart VM Service в dev-режиме на предмет исключений/зависших isolate, или сравнить с чистым `flutter create` demo-проектом в этом же окружении, чтобы понять — баг именно в этом приложении или в любом Flutter web приложении здесь). НЕ связано с миграцией темы (падает на уровне движка/бутстрапа, до любого моего Dart-кода) — не блокирует уверенность в корректности уже сделанной работы, только мешает её увидеть глазами.

**Если в следующей сессии будет доступен телефон/эмулятор** — это лучший способ реально увидеть тёмную тему: открыть Настройки → переключить тему → пройтись по всем 16 экранам + проверить, что `_OrderSheet`/тариф/карта по-прежнему выглядят как раньше (не должны были измениться вообще). Если снова нужен веб-путь — легаси-конфиг уже не проблема, ищи, почему нет первого кадра после `initializeEngine()` (возможно стоит проверить `AuthStore`/`ApiClient`/`SocketService` конструкторы в `main.dart`'s `_runApp()`, или сравнить с простым `flutter create` demo-приложением в этом же окружении, чтобы понять — это специфика ЭТОГО приложения или вообще любого Flutter web приложения в этой песочнице).

Также мигрированы (бонусом, раз уж проверял) `_TripDetailScreen` и `_LocationRequiredScreen` — у каждого СВОЙ независимый `Scaffold` (не общий с остальными 16 экранами), так что безопасно было сделать сразу (commit `7d9082f`). Их общие виджеты `_TripRouteMiniCard`/`_TripInfoPill`/`StatusPill` намеренно НЕ трогал — они используются и в live-трекинге поездки (`_TripStatusPanel` и т.д.), чью готовность к миграции я не проверял.

Плюс `_RegionConfirmSheet`/`_RegionSelectSheet` (commit `37aa8af`) — не входили в исходный чек-лист, но нашёл при аудите оставшихся `SmartTaxiColors` и оказались точно такими же самодостаточными шторками.

**Аудит live-трекинга поездки (searching/driver-found/en-route/in-progress/rating/receipt/cancelled — `_TripStatusPanel` и все его под-панели) — проверено, НЕ трогать, но не потому что "не проверял", а потому что теперь ПРОВЕРЕНО и подтверждено безопасно-как-есть:** все эти под-панели (`_RateDriverPanel`, `_TripReceiptPanel`, `_NoDriversFoundPanel`, `_DriverPriceOfferPanel`, `_TripCancelledPanel`, `_RatedThankYouPanel`, in-progress-панель) оборачиваются в `_HomeOrderPanel` — тот же самодостаточный белый `Container`, который используется и внутри защищённого `_OrderSheet`. Мигрировать что-то ВНУТРИ этих панелей (например, `_DriverContactCard`, который тоже сам себя красит в `Colors.white`) без миграции `_HomeOrderPanel` создало бы НОВОЕ визуальное рассогласование (тёмная карточка внутри белой панели) — хуже текущего единообразного "всё бело". А мигрировать сам `_HomeOrderPanel` нельзя, не тронув `_OrderSheet`. Итог: вся эта ветка остаётся полностью светлой в тёмной теме — сознательный, а не забытый компромисс.

**Код-ревью пройден (без визуальной проверки, только статический анализ):**
- Полный список `await ...; setState(...)` без `if (!mounted) return` — найден и исправлен ОДИН реальный баг: `_submitDriverApplication` (commit `eaf07d3`) — setState после await без guard, плюс catch-блок тоже был без guard, при этом finally-блок в той же функции guard уже имел (несогласованность). Остальные ~40 подобных мест в файле — уже корректно защищены.
- `TextEditingController`/`AnimationController`/`Timer` — все создания сверены с `dispose()`/`cancel()`, утечек не найдено.
- `TODO`/`FIXME`/`HACK` — ноль в `passenger_shell.dart` и `main.dart`.

**Все 3 `Scaffold(`-инстанса в `passenger_shell.dart` теперь мигрированы.** Пассажирская тема — готова полностью (за задокументированными выше исключениями: protected tariff/address-flow, cross-cutting core/-виджеты, map glass chrome).

Следующий шаг: `driver_shell.dart` — та же болезнь (статичные `SmartTaxiColors`), вероятно та же ловушка с корневым Scaffold. Проверь так же (найди все Scaffold-инстансы, пойми что на них висит) прежде чем трогать, и только когда сессия "SmartTaxi driver app features" подтверждённо неактивна — **проверено скриншотом с реального устройства (IBOVEMHQBQBQMJTS) в этом цикле: телефон активно используется, на экране был водительский экран сообщения о дорожном событии (тёмная тема, хороший контраст — что само по себе обнадёживает насчёт качества `SmartTaxiPalette.dark`), так что сессия ещё активна на момент написания. Проверь свежим скриншотом (`adb devices`, затем `adb shell screencap`) перед тем как трогать driver_shell.dart — если снова видно активную водительскую сессию, не трогай ни телефон, ни этот файл.**

## Дальше — общая полировка

Пройдись по всему пассажирскому опыту (карта, тарифы, поиск водителя, чат, история, настройки, FAQ, правовая информация) и по водительскому (после того как чат "SmartTaxi driver app features" закончит и не будет активен) с придирчивым взглядом дизайнера: отступы, тени, состояния загрузки/ошибки/пустых списков, читаемость в обеих темах, доступность (контраст, размеры тап-зон). Каждую находку — фиксируй и чини, а не просто отмечай.

## Границы

- Никогда не удаляй чужую незакоммиченную работу без крайней необходимости — сначала проверяй `git status`, если что-то не твоё, коммить отдельно с понятным сообщением или оставь как есть и опиши в статус-доке.
- Секреты (API-ключи, токены, пароли) никогда не пиши в код/коммиты — только через переменные окружения Railway.
- Живая проверка на телефоне — если он свободен (`adb devices` показывает устройство и `adb shell input tap` не падает с `SecurityException`); если недоступен — работай через `flutter analyze`/`flutter test`, честно помечай что не проверено вживую.
